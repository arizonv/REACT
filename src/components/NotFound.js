// Importar React si aún no lo has hecho
import React from "react";

// Definir el componente NotFound
function NotFound() {
  return (
    <div>
      <div className="container">
        <h1>NotFound</h1>
      </div>
    </div>
  );
}

// Exportar el componente NotFound
export default NotFound;
